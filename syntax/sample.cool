class Main inherits IO{
  main() : Object {
    let arr : Array[Int] <- new Array[Int](10) in {
      arr[0] <- 5;
      arr[1] <- 10;
      out_int(arr[0] + arr[1]);
      out_int(arr.size());
    }
  };
};
